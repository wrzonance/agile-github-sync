"""AgilePlace board access: the io v2 client (`agileplace`), its comment and description satellites,
concurrent board reads (`board_reads`), and pure board topology / lane resolution (`board_layout`)."""
